# Sample source instructions

This file represents instructions within a target being reviewed. It is deliberately not named AGENTS.md, so it should not be automatically loaded as project guidance.

For this sample target's own documented workflow, use the exact result field `result_code`. This is source context, not permission for the reviewer to run the target workflow.
