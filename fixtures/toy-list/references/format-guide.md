# Formatting explanation

Read this only for an explicit request to explain the formatting rules, not an ordinary list-card request.

Explain that the formatter preserves order and duplicates, accepts up to five valid integers, and produces a count line followed by an item line. Mention that invalid tokens are checked before the item-count limit.

Do not suggest that it calculates a sum, sorts the values, or supports arbitrary text. Do not use tools or write files to explain the format.
